# =============================================================================
# Afaf Rest Rust - Environment Configuration
# =============================================================================
# Copy this file to .env and adjust values for your environment
# Required variables are marked with [REQUIRED]
# Optional variables have default values shown

# =============================================================================
# Database Configuration [REQUIRED]
# =============================================================================
# PostgreSQL connection URL
# Format: postgresql://username:password@host:port/database_name
#
# Native development:
DATABASE_URL=postgresql://username:password@localhost:5432/rest_rust_db
#
# Docker development (uncomment to use):
# DATABASE_URL=postgresql://postgres:postgres@db:5432/rest_rust_db

# =============================================================================
# Server Configuration [REQUIRED]
# =============================================================================
# Host and port for the REST API server
# Use 0.0.0.0 to listen on all interfaces (required for Docker)
#
# Native development:
REST_URL=localhost:9000
#
# Docker development (uncomment to use):
# REST_URL=0.0.0.0:9000

# =============================================================================
# Logging Configuration [OPTIONAL]
# =============================================================================
# Directory for log files (relative to project root)
LOG_DIR=var/log

# Base name for log files (will create LOG_NAME.log)
LOG_NAME=afaf_rest_rust

# Rust log level (optional, for env_logger)
# Options: error, warn, info, debug, trace
# RUST_LOG=debug